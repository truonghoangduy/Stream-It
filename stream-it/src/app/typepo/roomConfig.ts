
export enum RoomViewMode {
  FOCUS,
  ALL,
}

export interface DialogRoomData {
  title: string;
  mesages: string;
}

export enum ButtonType {
  CAMERA,
  MICROPHONE,
  CALL
}

export enum VideoState {
  ON,
  OFF,
  NULL
}